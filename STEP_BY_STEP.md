# いまやること（2つだけ）

GAS の設置はもうできている前提です。  
**① 他の人がエラーなく開けるようにする** と **② QRコードを作る** だけ進めましょう。

---

## ① 他の人がエラーなく入れるようにする

### 1. デプロイの「共有」を確認する

1. [Google Apps Script](https://script.google.com) で、酒場みよのプロジェクトを開く
2. 右上 **「デプロイ」→「デプロイを管理」** をクリック
3. いま使っているデプロイ（ウェブアプリ）の **「編集」**（ペンアイコン）をクリック
4. 次を確認：
   - **「次のユーザーとして実行」** → **「自分」**（あなたのGoogleアカウント）
   - **「アクセスできるユーザー」** → **「全員」**
5. 「全員」になっていなければ **「全員」** に変更して **「デプロイ」** を押す  
   （「全員」でないと、Googleにログインしていないお客さんが開いたときにエラーになります）

### 2. 他の人でも開けるかテストする

- **スマホ** で、注文ページのURLを開く（そのスマホのGoogleアカウントでログインしていない状態、またはシークレット窓で）
- または **別の人のスマホ** で同じURLを開く  
→ 「お席を選んでください」やメニューが表示されればOK。  
ログインを求めたり「このアプリにアクセスできません」と出る場合は、上記「全員」になっているかもう一度確認してください。

---

## ② QRコードを作る

### 方法A: 管理画面のQRタブをそのまま印刷する（いちばん簡単）

1. ブラウザで **管理画面** を開く  
   （注文用URLの末尾に `?page=admin` を付けたURL）
2. **「📱 QRコード」** タブをクリック
3. 共通用・席別（A1, A2, …）のURLとQR画像が表示される
4. **「このページを印刷」** ボタンを押す → 印刷プレビューが開く
5. プリンターで印刷するか、「PDFに保存」を選んで保存  
   → その紙やPDFをテーブルに貼る・お客さんに渡す

### 方法B: URLをコピーしてQRコード作成サイトで作る

1. 管理画面の **「📱 QRコード」** タブを開く
2. 使いたい席（例: A1）の **「コピー」** をクリック
3. [QRコード生成サイト](https://www.google.com/search?q=qrコード+生成+無料) などで、コピーしたURLを貼り付けてQRコードを生成
4. 画像をダウンロードして印刷したり、名刺・卓上POPに貼る

### 席ごとに違うQRにしたい場合

- **共通QR**（1枚だけ）  
  → スキャンすると「お席を選んでください」→ 席選択 → 注文
- **席別QR**（A1用・A2用・…）  
  → スキャンすると、その席で注文画面がすぐ開く（席選択なし）

管理画面のQRタブに、共通用と席別の両方のURL＆QR画像が出ているので、必要な方を使ってください。

### 📱 iPhoneの標準カメラで読めるようにする（推奨）

iPhoneの「カメラ」アプリでQRを読むと、長いURLだと読み取りに失敗することがあります。**短いURL** をQRにすると読み取りやすくなります。

1. **短縮URLを作る**  
   [bit.ly](https://bit.ly) や [TinyURL](https://tinyurl.com) などで、**注文ページの本のURL**（管理画面QRタブに表示されている「元のURL」）を短縮します。  
   - 共通用が1本、席別（A1〜B2）が7本あるので、**計8本**の短縮URLを作成します。
2. **管理画面で登録する**  
   管理画面の「📱 QRコード」タブ上部の **「iPhone標準カメラで読めるようにする」** の欄に、作った短縮URLを貼り付け、「短縮URLを保存」を押します。
3. **QRを印刷し直す**  
   このあと表示されるQRコードは短いURL用になっています。もう一度「このページを印刷」で印刷するか、席別QRをコピーしてQR画像を作り直してください。

短縮URLは「元の注文URLへリダイレクトする」だけなので、お客さんが開くと通常どおり注文ページが表示されます。iPhoneのカメラでスキャンしても開けるようになります。

### 「ファイルを開けません」が出る場合（iPhone標準カメラ・Instagram）

iPhoneの**標準カメラ**や**Instagram**で開くと「現在、ファイルを開くことができません」「アドレスを確認して…」と Google ドライブの案内が出ることがあります。**script.google.com にアクセスした時点で Google 側でエラーになっており、アプリのコードは動いていません。** 短縮URLや gateway=1 では直りません。

**対処: 必ず「外部のリダイレクト用ページ」を使う**

QRコードの行き先を **script.google.com ではなく、あなたが置いた1枚のHTML** にします。

1. プロジェクトの **`redirect.html`** を開き、中の **`GAS_BASE`**（2か所）をあなたのGASのURL（`https://script.google.com/.../exec` まで、`?` は付けない）に書き換えます。
2. この **redirect.html を GitHub Pages や Netlify など、自分で用意したサイト**にアップロードします。  
   - GitHub Pages の例: リポジトリの Settings → Pages で有効にし、このファイルを `index.html` か `redirect.html` で置く。
3. QRコード（または短縮URLの行き先）を、**そのサイトのこのページ**にします。  
   - 例: `https://あなたのユーザー名.github.io/リポジトリ名/redirect.html?seat=A1`  
   - 席別なら `?seat=A1` の部分だけ変えて、同じ redirect ページを使い回せます。
4. お客さんがQRを読むと、**まずあなたのサイトのページだけが開き**、そのページから注文用の script.google.com へ飛びます。iPhoneのカメラやInstagramでも「ファイルを開けません」になりにくくなります。

---

## チェックリスト

- [ ] デプロイの「アクセスできるユーザー」が **「全員」** になっている
- [ ] 自分以外の端末（またはログインしていない状態）で注文URLを開いて、エラーなく表示されることを確認した
- [ ] 管理画面の「📱 QRコード」タブを開き、共通 or 席別のQRを **印刷** または **コピーしてQR作成** した
- [ ] （任意）iPhoneの標準カメラで読ませたい場合は、短縮URLを登録してQRを短いURL用にし直した

ここまでできていれば、お客さんがQRで開いてもエラーにならず、そのまま注文できます。
