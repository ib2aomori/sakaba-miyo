# 新規スプレッドシート作成後の手順（Step by Step）

「一献」メニューから「新規スプレッドシートを作成」を実行したあとに行うことを、順番にまとめています。

---

## Step 1: 作成されたスプレッドシートを確認する

1. GAS の **表示 → ログ** を開く（または実行時のログを確認）
2. ログに **「完了 URL: https://docs.google.com/...」** と **「ID: ...」** が出ていることを確認
3. その URL をブラウザで開き、次のシートがあることを確認
   - **sessions**（セッション管理）
   - **orders**（注文ログ）
   - **day_closings**（売上締め）
   - **menu**（メニュー・サンプル品が入っている）

※ この時点でスクリプトプロパティ **SS_ID** に、このスプレッドシートの ID が保存されています。

---

## Step 2: GAS プロジェクトに HTML をそろえる

1. **Google Apps Script** のエディタを開く（https://script.google.com で、このプロジェクトを開く）
2. 左のファイル一覧で次があるか確認する
   - **Code.gs**（または Code_latest.gs の内容を貼った Code.gs）
   - **Order.html**（Order_latest.html の内容を貼ったもの）
   - **Admin.html**（Admin_latest.html の内容を貼ったもの）
3. ない場合は **＋** から「HTML」を追加し、ファイル名を **Order** / **Admin** にし、ローカルの **Order_latest.html** / **Admin_latest.html** の内容をそのまま貼り付けて保存

※ スクリプトが「スプレッドシートに紐づいている」場合は、**拡張機能 → Apps Script** から開いたプロジェクトで、同じように Code.gs / Order.html / Admin.html を用意する。

---

## Step 3: ウェブアプリとしてデプロイする

1. GAS エディタで **「デプロイ」→「新しいデプロイ」** をクリック
2. 種類で **「ウェブアプリ」** を選ぶ
3. 次のように設定する
   - **説明**: 任意（例「一献 注文システム」）
   - **次のユーザーとして実行**: **自分**
   - **アクセスできるユーザー**: お客さん・店舗スタッフが開くなら **「全員」**
4. **「デプロイ」** を押す
5. **「ウェブアプリの URL」** が表示されるので、これをコピーしてメモ（例: `https://script.google.com/macros/s/xxxxx/exec`）

※ 初回は「承認」を求められるので、表示に従って許可する。

---

## Step 4: 動作確認（お客さん用・管理画面）

1. **お客さん用**
   - ブラウザで `[Step 3のURL]?seat=A1` を開く
   - メニューが表示され、席が「A1席」になっていれば OK
   - テストで 1 品だけ注文して「注文する」を押し、完了画面まで表示されるか確認
2. **管理画面**
   - ブラウザで `[Step 3のURL]?page=admin` を開く
   - 「営業中」タブに、今のテスト注文（A1席）が出ていれば OK
   - 「日別売上」「売上締め」タブも開いて表示を確認

---

## Step 5: 日替わりメニューを使う場合だけ（任意）

menu シートで「その日だけ表示する品」を出し分けたいときだけ実行する。

1. GAS エディタで関数一覧から **addMenuApplyDateColumn** を選ぶ
2. **実行** を 1 回だけ行う
3. 作成したスプレッドシートの **menu** シートを開き、右端に **「適用日」** 列が増えていることを確認
   - 適用日が **空** → 毎日表示
   - 適用日に **日付（yyyy/MM/dd）** を入れる → その日だけ表示

---

## Step 6: 席用の URL をメモする（QR コード用）

席は **A1, A2, A3, A4, A5, B1, B2** の 7 席です。各席用の URL は次のとおりです。

- A1: `[Step 3のURL]?seat=A1`
- A2: `[Step 3のURL]?seat=A2`
- A3: `[Step 3のURL]?seat=A3`
- A4: `[Step 3のURL]?seat=A4`
- A5: `[Step 3のURL]?seat=A5`
- B1: `[Step 3のURL]?seat=B1`
- B2: `[Step 3のURL]?seat=B2`

管理画面（店舗用）: `[Step 3のURL]?page=admin`

QR コードは、無料の QR コード生成サイトなどで「上記 URL」を入力して作成し、各席のテーブルに貼るか表示する。

---

## Step 7: メニュー・価格をスプレッドシートで調整する

1. 作成したスプレッドシートの **menu** シートを開く
2. **name**（商品名）・**price**（価格）・**is_active**（TRUE=表示、FALSE=非表示）を店のメニューに合わせて編集
3. 行を追加すれば品目を増やせる（カテゴリは「ドリンク」「フード」など）
4. 保存すると、次に注文画面を開いたときから反映される（キャッシュで古いままの場合は、QR の URL に `&v=2` などを付けて更新）

---

## まとめチェックリスト

- [ ] Step 1: スプレッドシートとシート構成を確認した
- [ ] Step 2: Code.gs / Order.html / Admin.html を GAS にそろえた
- [ ] Step 3: ウェブアプリとしてデプロイし、URL をメモした
- [ ] Step 4: お客さん用（?seat=A1）と管理画面（?page=admin）で動作確認した
- [ ] Step 5: 日替わりメニューを使う場合だけ addMenuApplyDateColumn を実行した
- [ ] Step 6: 各席用 URL をメモし、QR コードを作成した
- [ ] Step 7: menu シートでメニュー・価格を調整した

ここまでできていれば、店舗でお客さんに QR を読んでもらい、注文→管理画面で確認→会計・売上締め、という流れで運用できます。
