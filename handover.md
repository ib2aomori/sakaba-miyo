# 一献（いっこん）注文システム 引き継ぎ文書

## プロジェクト概要

和風居酒屋「一献（いっこん）」向けのQRコード注文・管理システム。
Google Apps Script（GAS）のウェブアプリとして実装。バックエンド・フロントエンド全てGASで完結。
スプレッドシートをデータベース代わりに使用。月額コスト0円。

---

## システム構成

### ファイル構成（GASプロジェクト内）

```
Code.gs       サーバーサイド。全APIロジック・ルーティング
Order.html    お客さん用注文画面
Admin.html    店主用管理画面
```

### スプレッドシート構成（データベース）

```
menu          メニューマスタ（商品名・価格・飲み放題対象フラグ・表示フラグ）
sessions      セッション管理（席・開始時刻・会計状況・飲み放題フラグ）
orders        注文ログ（セッションID・商品・数量・金額）
day_closings  売上締め記録
```

### デプロイ情報

- デプロイURL: `https://script.google.com/macros/s/AKfycbwLewvZN2SCf3vEY4gfPOz7NrnyA-DBHlXcc79dIybEk6pxmQ6LvIxpFiht7w-cc3hA/exec`
- お客さん用: `[URL]?seat=A1`（A1〜A5、B1〜B2）
- 管理画面: `[URL]?page=admin`

---

## 席構成

A1, A2, A3, A4, A5, B1, B2（計7席）

---

## メニュー構成

### ドリンク（飲み放題対象：○）

| 商品名 | 価格 | 飲み放題対象 |
|---|---|---|
| 生ビール | ¥550 | ○ |
| ハイボール | ¥480 | ○ |
| 梅酒ソーダ | ¥480 | ○ |
| 緑茶ハイ | ¥480 | ○ |
| ウーロンハイ | ¥480 | ○ |
| 日本酒（冷・1合） | ¥680 | × |
| 焼酎水割り | ¥480 | ○ |
| ソフトドリンク | ¥300 | × |

### フード

| 商品名 | 価格 |
|---|---|
| 枝豆 | ¥280 |
| 唐揚げ（5個） | ¥580 |
| 焼き鳥盛り合わせ（5本） | ¥780 |
| だし巻き玉子 | ¥480 |
| 冷奴 | ¥320 |
| 海鮮サラダ | ¥680 |
| 鶏の塩焼き | ¥680 |
| 肉じゃが | ¥580 |
| たこわさ | ¥480 |
| 〆の鶏雑炊 | ¥480 |

※ メニューはスプレッドシートのmenuシートで管理。コードを触らずシートを編集するだけで即反映。

---

## 実装済みの機能

### お客さん用画面（Order.html）
- QRコードから開くと席番号が自動入力済み
- カテゴリ別メニュー表示（ドリンク・フード）
- 数量ボタン（＋/−）で選択
- 追加注文は何度でも可能（同一セッションに紐づく）
- Googleアカウント不要
- 備考・アレルギー入力欄

### 飲み放題機能（Order.html）
- 管理画面でセッションに飲み放題フラグをセット
- 最初の注文時刻から自動で120分計測開始
- 画面上部にカウントダウンバナー表示（残り時間・LOまでの時間）
- LO（90分）まで：対象ドリンクが¥0表示・「飲放」バッジ
- LO超過後：対象ドリンクが定価に自動切り替わり・「LO終了」バッジ
- 120分超過後：対象ドリンクがグレーアウトして注文不可

### 管理画面（Admin.html）
- 15秒ごとに自動更新
- 新規注文が入ると赤いバナー通知＋ビープ音
- 営業中タブ：全席の注文明細を常時展開表示（タップ不要）
- ドリンクは赤字・赤丸、フードは緑丸で色分け
- 同席に複数グループが来た場合「2回転目」バッジで識別
- 飲み放題セットボタン（席ごと）
- 飲み放題の残り時間をリアルタイム表示
- 提供済みボタン（会計前の確認用）
- 会計済みにするボタン（人数選択→クローズ）
- 本日の履歴タブ：会計済みセッション一覧
- 売上締めタブ：本日の売上サマリー＋営業終了ボタン

### データ設計のポイント
- セッション管理：同じ席でも会計ごとに別セッション（A1-1423形式のID）
- unit_priceを注文時点で確定保存（後からメニュー価格を変えても過去ログに影響しない）
- 飲み放題中は¥0で注文データを保存
- menuシートのis_activeをFALSEにするだけで非表示（行削除不要）

---

## 未解決のバグ・課題

### ① 売上締めボタンのバグ（最優先）
**現象**: 「本日の売上を締めますか？」→「はい」を押しても、営業中タブのセッションが閉じられない。
**原因**: `closeDaySales()`がday_closingsシートに記録するだけで、sessionsシートのstatusを"closed"に変更していない。
**修正方針**: closeDaySalesでオープン中の全セッションのstatus="closed"、closed_at=現在時刻をセットする。管理画面側でも締め後にloadAll()を呼んで画面を更新する。

### ② ブラウザキャッシュ問題
**現象**: menuシートの価格を変えてもお客さん用画面に反映されない。
**原因**: GASウェブアプリをブラウザが強くキャッシュする。シークレットウィンドウでは正しく表示される。
**修正方針**: URLにバージョンパラメータ（?v=1など）を付けてキャッシュを回避。または、Cache-Controlヘッダーの設定。

### ③ 飲み放題をお客さん側でセットする機能（未実装）
**現状**: 管理画面から店員が飲み放題フラグをセットする方式。
**要望**: お客さん自身が注文画面で飲み放題コースを選べるようにしたい。
**設計方針案A**: 「飲み放題コース ¥〇〇」をメニューに追加し、注文した瞬間に自動でフラグが立つ。
**設計方針案B**: QRコードを2種類作る（通常用・飲み放題用、URL末尾に?nomi=1）。

### ④ 日替わりメニューへの対応（基本設計は完了）
menuシートで管理できるようになったが、商品名を変えると過去ログとの突合ができなくなる。行を追加する運用を推奨しているが、UIでの案内が不十分。

---

## 今後追加したい機能（優先度順）

1. 売上締めバグの修正（上記①）
2. キャッシュ問題の恒久対応（上記②）
3. お客さん側での飲み放題選択（上記③）
4. 週次・月次の売上ダッシュボード（KPI: 客単価推移・曜日別売上・人気メニューランキング）
5. メニュー管理画面（Admin.htmlからmenuシートを直接編集できるUI）
6. 会計時にレシート的なサマリーをお客さんに表示

---

## 技術的な注意事項

### GAS固有の制約
- `google.script.run`でクライアント→サーバーの非同期通信
- HTMLファイルはサーバーサイドレンダリング（テンプレートで変数を注入可能）
- 実行時間上限: 6分/回
- ウェブアプリのデプロイ更新後もブラウザキャッシュが残る
- `PropertiesService`でスプレッドシートIDを保存（環境変数代わり）

### セッション列マップの安全設計
スプレッドシートの列が増えても壊れないよう、`getSessionColMap()`でヘッダー行を動的に読んで列インデックスを取得している。ハードコードしていない。

### スプレッドシートID
スクリプトプロパティ`SS_ID`に保存。`setSsId()`関数で手動登録も可能。

---

## Cursorへの依頼事項

上記のバグと機能を引き継いで開発を続けてほしい。
添付する3ファイル（Code.gs・Order.html・Admin.html）が現在の最新版。
GASプロジェクトに貼り付けてデプロイすれば動作する状態。

開発の進め方：
- コードを修正したらGASエディタに貼り付けてデプロイ（このステップだけ手動）
- デプロイURLは上記の通り固定
- スプレッドシートのデータは既存のものを使い続ける
